// index.js

describe('Page Load Test', function() {

    it('Page should load and render main sections of the page', function() {
  
      // We want tests to pass.
      if (5 == 3) {
        // Hope we don't get here.
        throw new Error("Oh no.");
      }
  
      // Since no error thrown, test will pass.
  
    });
  
  });